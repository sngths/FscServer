<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianxing.database.mapper.UserMapper">
    <insert id="createStudent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, userType)
        VALUES (#{username}, #{password}, 's')
    </insert>

    <insert id="createTeacher" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, userType)
        VALUES (#{username}, #{password}, 't')
    </insert>


</mapper>